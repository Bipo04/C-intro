#include <stdio.h>
#include <limits.h>
#include <float.h>

int main() {
    printf("The range of int is from %d to %d\n", INT_MIN, INT_MAX);
    printf("The range of long is from %ld to %ld\n", LONG_MIN, LONG_MAX);
    return 0;
}